{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Create Activity{% endblock %}

{% block body %}
    {{ form_errors(activityForm) }}

    {{ form_start(activityForm) }}
    {{ form_widget(activityForm) }}


    {{ form_end(activityForm) }}
{% endblock %}